// Minimal bench target to keep the workspace compiling under `cargo test --all-targets`.
// Real benchmarks can be reintroduced later, potentially under a feature flag.

fn main() {
    // Benchmarks retired for now. Enable and implement when needed.
}
